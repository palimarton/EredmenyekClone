<UserControl
    x:Class="Eredmenyek.Views.IncidentUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Eredmenyek.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <RelativePanel>
        <StackPanel x:Name="StackPanel" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image x:Name="IncidentImage" Width="20" Height="20" />
            <TextBlock xml:space="preserve" LineHeight="15" VerticalAlignment="Stretch" FontSize="15" Height="20">
                <Run Text="   " /><Bold><Run Text="{Binding ElapsedTime}" />'</Bold><Run Text="   " /><Run Text="{Binding Text}" />                
            </TextBlock>
        </StackPanel>
    </RelativePanel>
</UserControl>
